# 최신 main 한글·영어 재점검

- 점검일: 2026-09-21
- 로컬 브랜치: `main`. fetch 후 HEAD와 origin/main 모두 `d2895d5`.
- 공간 콘텐츠 제외. 로그인, AI 3개 화면, 나들이 6개 상태 및 기본 코스 상세 15개, 내 정보 7개, 택시 설정, 홈, 렌트 3개 단계 및 차량 5종: 총 42개 상태.
- 임시 Edge 프로필, 430×932 화면에서 한글→영어→한글 DOM 비교. 별도 로컬 서버로 현재 public 파일을 제공했다.
- 결과: 영어에서 한글이 남은 **고유 텍스트 38개**, **placeholder·접근성 속성 10개**, **한글을 포함한 배너 이미지 3종**. 숫자·이름이 다른 문자열은 별도 집계했으며, 이것이 모든 미번역 문구의 총수라는 의미는 아니다.
- 42개 상태 모두 한글로 되돌리면 수집한 텍스트가 원래대로 복구됐다.

## 1. 나들이: 텍스트 28개

위치: `front/public/moov.html`의 `renderOuting`(2734행), `renderOutingMap`(2745행), `renderCourseBrowse`(2852행), `buildCourseMap`의 안내(2937행), `renderRecommendation`(3014행), `renderInterestCourses`(3085행).

| 화면 | 영어에서도 한글로 남는 문구 |
| --- | --- |
| 상단 메뉴 | 커뮤니티 / 코스등록 / 관심코스 |
| 정렬 | 취향순 / 거리순 |
| 코스 카드 | 최근 7일 — / 가격·시간 확인 필요 |
| 관심 코스 | 관심 저장한 코스를 한곳에서 확인하세요. |
| 추천 입력 | Audit님의 선택을 바탕으로 검수 전 데모 코스를 비교합니다. / 참고 예산 / 100,000원 이하 |
| 추천 입력 안내 | 데모 비용 기준입니다. 실제 가격·이동비·영업시간 충족을 보장하지 않습니다. |
| 동기화 안내 | 전송 대기 기록 0건 · 백엔드가 켜지면 같은 ID로 재시도합니다. |
| 추천 결과 | 추천 결과 / 선택 CATEGORY와 데모 태그를 비교한 임시 순위입니다. 실제 장소 운영시간·가격·동선은 검증되지 않았어요. |
| 추천 점수 | 1순위 · 매칭 100% / 2순위 · 매칭 0% / 3순위 · 매칭 0% / 취향 매칭 / 거리 근접 |
| 지도 | 15개 결과 / 지도 초기화 / 15개 코스 |
| 지도 안내 | 14개 코스는 일부 장소의 좌표가 없어 지도 경로가 표시되지 않아요. 지도에 표시된 좌표는 검수 전 데모 위치입니다. |
| 상세 팝업 | 장소 위치 3/3개 확인 가능 / 장소 위치 0/3개 확인 가능 / 장소 위치 1/3개 확인 가능 |
| 상세 위치 안내 | 확인 가능한 좌표는 검수 전 데모 위치입니다. 위치가 없는 장소는 임의로 지도에 찍지 않습니다. |

`Audit`은 검사에 사용한 임시 사용자 이름이다. 순위·점수·개수는 테스트 상태의 예시다. 추천 실행이나 기록 전송 없이 기본 코스 3개를 결과 상태에 배치해 결과 표시를 검사했다.

상단 메뉴는 영어 문구가 소스에 있는데도 적용되지 않는다. 버튼의 `data-i18n-en`과 내부 span의 텍스트가 분리되어 있고, `front/public/i18n.js:157`의 `translateText`는 직접 부모의 속성만 읽기 때문이다.

추가 정적 확인: 추천 점수의 `시간 여유`, `영업시간`도 번역 사전에 없다. 이번 기본 상태에서는 해당 점수가 null이라 화면에 표시되지 않았으므로 위 28개에는 넣지 않았다.

## 2. 택시 설정: 텍스트 7개

위치: `front/public/moov.html:2206`의 `renderVehicleTypeSelector` 등.

- 서울 성수동
- 22,000원/시간
- 29,000원/시간
- 34,000원/시간
- 32,000원/시간
- 최대 6인 · 가족·짐이 많은 여행용
- MOOV 컴팩트 · 약 3분

## 3. 렌트: 텍스트 2개

위치: `front/public/moov-home/js/home.js`의 `renderBooking`.

- 최소
- 최대

차량 5종 모두 동일하게 남는다. 시간과 차량 요금 등 다른 일반 문구는 검사한 상태에서 번역된다.

## 4. AI: 텍스트 1개

- `9월 21일 (월)` — 이미 열린 대화 화면에서 영어로 전환하면 날짜가 한글로 남는다. `renderAi`가 화면 생성 시 locale을 사용하므로 영어 상태에서 새로 렌더하는 경우와 구분해야 한다.

## 5. 입력 안내·접근성 속성: 10개

| 화면 | 속성 | 한글로 남는 내용 |
| --- | --- | --- |
| 코스 등록 | placeholder | 코스를 소개해 주세요 |
| AI 캐릭터 | alt | 무브 캐릭터 |
| AI 설정 | aria-label | 루나 전환 |
| AI 설정 | aria-label | 음성으로 대화하기 전환 |
| AI 설정 | aria-label | 승차 후 자동 시작 전환 |
| AI 설정 | aria-label | 대화 기록 저장 전환 |
| 추천 예산 | aria-label | 추천 참고 예산 |
| 내 정보 | alt | 프로필 사진 |
| 택시 지도 | aria-label | 택시 배차 지도 |
| 렌트 지도 | aria-label | 예약 코스 지도 |

코스 소개 placeholder는 사용자에게 직접 보인다. 번역기가 TEXTAREA 자체를 건너뛰기 때문에 placeholder도 처리하지 않는다. 사용자 입력 내용을 보존하면서 안내 속성은 별도로 번역해야 한다.

## 6. 홈 배너: 이미지 3종

`front/public/moov-home/js/data.js:22`부터 음식점·전시·스파이더맨 배너의 `image`와 `imageEn`이 같은 파일을 가리킨다. 영어에서도 한글이 포함된 이미지가 그대로 사용된다. 배너 밖 제목·설명 번역과는 별개의 문제다.

## 이전 점검과의 차이 및 한계

- `4849376` 이후 공통 번역기와 공통·홈 번역 사전은 변경되지 않았다. 기존 누락이 남아 있고, 추천 점수 등 새 UI에서도 누락이 발견됐다.
- 이번에는 추천 결과, 지도, 코스 상세, 차량별 화면으로 범위를 넓혔으므로 이전 보고서의 문구 수·비율과 직접 비교하면 안 된다.
- 로그인·홈 일반 텍스트·내 정보 일반 텍스트에서는 이번 표본에서 미번역을 찾지 못했다. 내 정보 이미지 alt와 홈 배너는 예외다.
- 기본 코스·테스트 상태의 표시를 검사했다. DB 전체 코스·사용자 작성 내용, 모든 모달과 서버 오류, 실제 AI 응답·음성, 모든 배차 단계, 화면 잘림은 전수 검사하지 않았다.
- 앱 소스와 환경설정 파일은 수정하지 않았다. 수정 우선순위는 나들이 메뉴 및 새 추천 결과 → 코스 등록 placeholder → 홈 배너 → 렌트·택시 문구 → 날짜·접근성 설명이다.
